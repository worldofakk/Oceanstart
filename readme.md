# Omelyanenko Vlad

## Setup

```bash
# install dependencies
$ npm install

# install gulp-cli
$ npm install -g  gulp-cli

# dev with hot reload at localhost:3000
$ gulp dev

# build for production
$ gulp build
```
